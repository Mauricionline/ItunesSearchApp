import { SearchForm } from "../../components/SearchForm/SearchForm"

const SearchPage = () => {

    return (
        <>
            <SearchForm onSearch={() => { }} loading={false} />
        </>
    )
}

export { SearchPage };